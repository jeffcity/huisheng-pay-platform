# 项目边界

- 本项目只维护汇盛支付平台端统一 Demo。
- `src/demo.html` 是唯一可编辑界面源文件。
- 平台端业务需求、待确认事项和专项 Demo 继续以 `../../三端需求管理/平台端/` 为准。
- 跨端业务规则以 `../../10-顶层业务设计/` 与 `../../20-技术契约/` 为准。
- 根目录 `index.html` 与 `dist/index.html` 是构建产物。
- `.github/workflows/pages.yml` 只在建立 GitHub 远端并启用 Pages 后执行发布。
